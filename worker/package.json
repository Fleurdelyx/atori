{
  "name": "atori-cloud",
  "private": true,
  "version": "0.1.0",
  "description": "ATRI cloud library worker — Cloudflare R2 streaming + manifest sync.",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250903.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
